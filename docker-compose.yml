version: '3.8'

services:
  mcp-db-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-db-server
    ports:
      - "${SERVER_PORT:-8088}:8088"
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped